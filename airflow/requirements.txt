# Core Airflow
apache-airflow[celery,crypto,postgres,ssh]==2.7.1

# Remove mysql from extras if using mysql-connector
# Use mysql-connector-python instead of mysqlclient
mysql-connector-python==8.3.0

# Celery stack
celery==5.3.6
kombu==5.3.6
billiard==4.2.0

# Spark
pyspark==3.0.1

# Database connectors
psycopg2-binary==2.9.9
pymongo>=3.6.0,<4.0.0

# Web utilities
tornado==6.4.1
werkzeug==2.3.8
SQLAlchemy==1.4.52
wtforms==3.1.2

# Kafka
kafka-python==2.0.2

# Utilities
python-dotenv==1.0.0
requests==2.31.0
pytz==2024.2
pendulum==2.1.2
Flask-Session>=0.4.0,<0.5.0
connexion<3

# Airflow providers (install separately after core)
apache-airflow-providers-apache-spark==3.0.0
apache-airflow-providers-mongo==3.0.0
apache-airflow-providers-postgres==4.0.1